{
  "name": "notification-pr-bot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "npx eslint index.js",
    "format": "npx prettier --write index.js",
    "build": "ncc build index.js -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/github": "^5.0.0",
    "js-base64": "^2.5.2",
    "yaml": "^1.10.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.27.0",
    "eslint": "^7.20.0",
    "prettier": "2.2.1"
  }
}
